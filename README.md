# AI XSA Governance Publisher

## Overview

Reads HANA XSA SQLScript, explains logic using Claude AI,
and publishes metadata to Collibra and Ellie.ai.

## Steps

1. Copy environment file:

cp .env.example .env

2. Add API keys.

3. Run:

docker-compose up

Outputs stored in:

outputs/semantic
